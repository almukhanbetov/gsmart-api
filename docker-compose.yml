services:
  api:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: gsmart-api

    network_mode: host
    environment:
      PORT: "8080"
      DATABASE_URL: "postgres://postgres:Zxcvbnm123@127.0.0.1:5432/smart24_db?sslmode=disable"

    restart: unless-stopped